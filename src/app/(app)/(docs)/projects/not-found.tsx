import { NotFound as PageNotFound } from "@/components/not-found";

export const metadata = {
  title: "Page Not Found | Projects",
};

export default function NotFound() {
  return <PageNotFound />;
}
